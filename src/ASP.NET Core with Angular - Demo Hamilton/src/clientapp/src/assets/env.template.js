(function(window) {
    window["env"] = window["env"] || {};
  
    // Environment variables
    window["env"]["GOOGLE_MAP_API_KEY"] = "${GOOGLE_MAP_API_KEY}";
    window["env"]["RANGE"] = "${RANGE}";
  })(this);